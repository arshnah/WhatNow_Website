import { FeaturedGuides, LanguageProvider } from "whatnow-web";

// Horizontally-scrolling row of featured career/exam guide cards.
export const Default = () => (
  <LanguageProvider>
    <FeaturedGuides />
  </LanguageProvider>
);
